interface:
  display_name: "CCF Experiment Env Design"
  short_description: "Audit scenario causes, plan balance, and tunable rules"
  default_prompt: "Use $ccf-experiment-env-design to check the scenario problem and causes, plan reasonableness, complexity balance, and whether tuned rules are forced to sacrifice one side of the intended tradeoff."
