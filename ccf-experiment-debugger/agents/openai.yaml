interface:
  display_name: "CCF Experiment Debugger"
  short_description: "Run evidence-led MES and algorithm repair loops"
  default_prompt: "Use $ccf-experiment-debugger to isolate this environment, MES, or algorithm-MVP failure, apply one owned delta per round, escalate exhausted algorithm repair to non-simplifying model review, and close invalidated checks with native independent review."
