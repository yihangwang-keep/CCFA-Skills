interface:
  display_name: "CCF Experiment Debugger"
  short_description: "Run evidence-led MVP repair and validation loops"
  default_prompt: "Use $ccf-experiment-debugger to isolate this environment or algorithm MVP failure, preserve the frozen research layers, apply one owned delta per round, and close every invalidated check with fresh domain audits and code review."
